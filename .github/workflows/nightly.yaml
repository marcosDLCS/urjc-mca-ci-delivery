name: 'Nightly'

on:
  schedule:
    - cron:  '1 3 * * *'

jobs:
  nightly_build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: Echo Nightly
        run: echo "Hello nightly!"